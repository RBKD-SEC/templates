id: rocketmq-detect

info:
  name: RocketMQ Detect
  author: internal-audit
  severity: info
  description: Detects RocketMQ Console panel.
  metadata:
    max-request: 1
  tags: fingerprint,http,rocketmq,middleware,internal-audit

http:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: word
        part: body
        words:
          - "RocketMQ"
          - "rocketmq"
        condition: or
