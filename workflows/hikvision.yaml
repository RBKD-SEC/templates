id: hikvision

info:
  name: Hikvision Scan
  author: internal
  description: |
    Service-specific scan for hikvision. Covers: fingerprint detection, authentication checks.
  tags: workflow,hikvision

workflows:
  - template: http/technologies/hikvision-detect.yaml
  - template: http/default-logins/hikvision-mini-brute.yaml
