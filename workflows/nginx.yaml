id: nginx

info:
  name: Nginx Scan
  author: internal
  description: |
    Service-specific scan for nginx. Covers: fingerprint detection, security exposure checks.
  tags: workflow,nginx

workflows:
  - template: http/technologies/nginx-detect.yaml
  - template: http/exposures/directory-listing.yaml
