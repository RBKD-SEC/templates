id: gitlab

info:
  name: Gitlab Scan
  author: internal
  description: |
    Service-specific scan for gitlab. Covers: fingerprint detection.
  tags: workflow,gitlab

workflows:
  - template: http/technologies/gitlab-detect.yaml
