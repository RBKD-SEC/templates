id: gitlab

info:
  name: Gitlab Scan
  author: internal-audit
  description: |
    Service-specific scan for gitlab. Covers: fingerprint detection.
  tags: workflow,gitlab

workflows:
  - template: fingerprints/http/devops/gitlab-detect.yaml
