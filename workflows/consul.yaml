id: consul

info:
  name: Consul Scan
  author: internal
  description: |
    Service-specific scan for consul. Covers: security exposure checks.
  tags: workflow,consul

workflows:
  - template: http/exposures/consul-unauth.yaml
