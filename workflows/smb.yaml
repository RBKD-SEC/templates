id: smb

info:
  name: Smb Scan
  author: internal
  description: |
    Service-specific scan for smb. Covers: fingerprint detection, security exposure checks.
  tags: workflow,smb

workflows:
  - template: network/detection/smb-detect.yaml
  - template: network/misconfig/smb-null-session.yaml
