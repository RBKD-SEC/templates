id: smb

info:
  name: Smb Scan
  author: internal
  description: |
    Service-specific scan for smb. Covers: fingerprint detection, security exposure checks.
  tags: workflow,smb

workflows:
  - template: network/technologies/smb-detect.yaml
  - template: network/misconfiguration/smb-null-session.yaml
