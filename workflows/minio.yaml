id: minio

info:
  name: Minio Scan
  author: internal
  description: |
    Service-specific scan for minio. Covers: fingerprint detection, authentication checks.
  tags: workflow,minio

workflows:
  - template: http/technologies/minio-detect.yaml
  - template: http/default-logins/minio-mini-brute.yaml
