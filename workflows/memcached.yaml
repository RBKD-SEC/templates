id: memcached

info:
  name: Memcached Scan
  author: internal
  description: |
    Service-specific scan for memcached. Covers: fingerprint detection, security exposure checks.
  tags: workflow,memcached

workflows:
  - template: network/technologies/memcached-detect.yaml
  - template: network/misconfiguration/memcached-unauth.yaml
