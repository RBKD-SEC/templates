id: memcached

info:
  name: Memcached Scan
  author: internal
  description: |
    Service-specific scan for memcached. Covers: fingerprint detection, security exposure checks.
  tags: workflow,memcached

workflows:
  - template: network/detection/memcached-detect.yaml
  - template: network/misconfig/memcached-unauth.yaml
