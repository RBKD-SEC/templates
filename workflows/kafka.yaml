id: kafka

info:
  name: Kafka Scan
  author: internal
  description: |
    Service-specific scan for kafka. Covers: fingerprint detection, security exposure checks.
  tags: workflow,kafka

workflows:
  - template: http/technologies/kafka-detect.yaml
  - template: http/exposures/kafka-connect-exposed.yaml
  - template: network/misconfig/kafka-unauth.yaml
