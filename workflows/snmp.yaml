id: snmp

info:
  name: Snmp Scan
  author: internal
  description: |
    Service-specific scan for snmp. Covers: fingerprint detection, security exposure checks.
  tags: workflow,snmp

workflows:
  - template: network/technologies/snmp-detect.yaml
  - template: network/misconfiguration/snmp-public-community.yaml
