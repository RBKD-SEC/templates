id: rsync

info:
  name: Rsync Scan
  author: internal
  description: |
    Service-specific scan for rsync. Covers: fingerprint detection, security exposure checks.
  tags: workflow,rsync

workflows:
  - template: network/technologies/rsync-detect.yaml
  - template: network/misconfiguration/rsync-anonymous-list.yaml
