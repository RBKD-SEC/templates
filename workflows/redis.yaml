id: redis

info:
  name: Redis Scan
  author: internal
  description: |
    Service-specific scan for redis. Covers: fingerprint detection, security exposure checks, authentication checks.
  tags: workflow,redis

workflows:
  - template: network/technologies/redis-detect.yaml
  - template: network/misconfiguration/redis-no-auth.yaml
  - template: network/default-logins/redis-acl-mini-brute.yaml
