id: redis

info:
  name: Redis Scan
  author: internal
  description: |
    Service-specific scan for redis. Covers: fingerprint detection, security exposure checks, authentication checks.
  tags: workflow,redis

workflows:
  - template: network/detection/redis-detect.yaml
  - template: network/misconfig/redis-no-auth.yaml
  - template: network/default-login/redis-acl-mini-brute.yaml
