id: mysql

info:
  name: Mysql Scan
  author: internal
  description: |
    Service-specific scan for mysql. Covers: fingerprint detection, authentication checks.
  tags: workflow,mysql

workflows:
  - template: network/detection/mysql-detect.yaml
  - template: network/default-login/mysql-mini-brute.yaml
