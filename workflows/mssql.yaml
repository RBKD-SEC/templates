id: mssql

info:
  name: Mssql Scan
  author: internal
  description: |
    Service-specific scan for mssql. Covers: fingerprint detection, authentication checks.
  tags: workflow,mssql

workflows:
  - template: network/detection/mssql-detect.yaml
  - template: network/default-login/mssql-mini-brute.yaml
