id: mssql

info:
  name: Mssql Scan
  author: internal
  description: |
    Service-specific scan for mssql. Covers: fingerprint detection, authentication checks.
  tags: workflow,mssql

workflows:
  - template: network/technologies/mssql-detect.yaml
  - template: network/default-logins/mssql-mini-brute.yaml
