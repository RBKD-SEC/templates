id: ge

info:
  name: Ge Scan
  author: internal
  description: |
    Service-specific scan for ge. Covers: fingerprint detection, authentication checks.
  tags: workflow,ge

workflows:
  - template: http/technologies/ge-detect.yaml
  - template: http/default-logins/ge-mini-brute.yaml
