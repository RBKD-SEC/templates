id: jenkins

info:
  name: Jenkins Scan
  author: internal
  description: |
    Service-specific scan for jenkins. Covers: fingerprint detection, security exposure checks, authentication checks.
  tags: workflow,jenkins

workflows:
  - template: http/technologies/jenkins-detect.yaml
  - template: http/exposures/swagger-ui-exposed.yaml
  - template: http/default-logins/jenkins-mini-brute.yaml
