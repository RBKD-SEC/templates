id: postgres

info:
  name: Postgres Scan
  author: internal
  description: |
    Service-specific scan for postgres. Covers: fingerprint detection, authentication checks.
  tags: workflow,postgres

workflows:
  - template: network/detection/postgres-detect.yaml
  - template: network/default-login/postgres-mini-brute.yaml
