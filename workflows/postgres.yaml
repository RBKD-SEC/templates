id: postgres

info:
  name: Postgres Scan
  author: internal
  description: |
    Service-specific scan for postgres. Covers: fingerprint detection, authentication checks.
  tags: workflow,postgres

workflows:
  - template: network/technologies/postgres-detect.yaml
  - template: network/default-logins/postgres-mini-brute.yaml
