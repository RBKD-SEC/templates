id: zookeeper

info:
  name: Zookeeper Scan
  author: internal
  description: |
    Service-specific scan for zookeeper. Covers: fingerprint detection, security exposure checks.
  tags: workflow,zookeeper

workflows:
  - template: network/detection/zookeeper-detect.yaml
  - template: network/misconfig/zookeeper-unauth.yaml
