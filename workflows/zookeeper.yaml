id: zookeeper

info:
  name: Zookeeper Scan
  author: internal
  description: |
    Service-specific scan for zookeeper. Covers: fingerprint detection, security exposure checks.
  tags: workflow,zookeeper

workflows:
  - template: network/technologies/zookeeper-detect.yaml
  - template: network/misconfiguration/zookeeper-unauth.yaml
