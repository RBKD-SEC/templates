id: ftp

info:
  name: Ftp Scan
  author: internal
  description: |
    Service-specific scan for ftp. Covers: fingerprint detection, security exposure checks, authentication checks.
  tags: workflow,ftp

workflows:
  - template: network/technologies/ftp-detect.yaml
  - template: network/misconfiguration/ftp-anonymous-login.yaml
  - template: network/default-logins/ftp-mini-brute.yaml
