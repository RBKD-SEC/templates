id: ftp

info:
  name: Ftp Scan
  author: internal
  description: |
    Service-specific scan for ftp. Covers: fingerprint detection, security exposure checks, authentication checks.
  tags: workflow,ftp

workflows:
  - template: network/detection/ftp-detect.yaml
  - template: network/misconfig/ftp-anonymous-login.yaml
  - template: network/default-login/ftp-mini-brute.yaml
