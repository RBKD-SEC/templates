id: jboss

info:
  name: Jboss Scan
  author: internal
  description: |
    Service-specific scan for jboss. Covers: fingerprint detection, security exposure checks.
  tags: workflow,jboss

workflows:
  - template: http/technologies/jboss-detect.yaml
  - template: http/exposures/jboss-jmx-console-exposed.yaml
