id: mongodb

info:
  name: Mongodb Scan
  author: internal
  description: |
    Service-specific scan for mongodb. Covers: security exposure checks.
  tags: workflow,mongodb

workflows:
  - template: network/misconfig/mongodb-no-auth.yaml
