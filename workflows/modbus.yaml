id: modbus

info:
  name: Modbus Scan
  author: internal
  description: |
    Service-specific scan for modbus. Covers: fingerprint detection.
  tags: workflow,modbus

workflows:
  - template: network/detection/modbus-detect.yaml
