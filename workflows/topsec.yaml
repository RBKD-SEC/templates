id: topsec

info:
  name: Topsec Scan
  author: internal
  description: |
    Service-specific scan for topsec. Covers: fingerprint detection, authentication checks.
  tags: workflow,topsec

workflows:
  - template: http/technologies/topsec-detect.yaml
  - template: http/default-logins/topsec-mini-brute.yaml
