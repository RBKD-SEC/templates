id: omron

info:
  name: Omron Scan
  author: internal
  description: |
    Service-specific scan for omron. Covers: fingerprint detection, authentication checks.
  tags: workflow,omron

workflows:
  - template: http/technologies/omron-detect.yaml
  - template: http/default-logins/omron-mini-brute.yaml
