id: middleware

info:
  name: Middleware Services Workflow
  author: internal-audit
  description: |
    Middleware fingerprinting + safe risk detection + optional login brute.
    Covers Tomcat, Spring Boot, Nginx, Apache.
    WARNING: optional templates may trigger account lockout or login alerts.
  tags: workflow,internal-audit,middleware,optional

workflows:
  - tags: fingerprint,middleware,internal-audit
  - tags: safe,middleware,internal-audit
  - tags: optional,middleware,internal-audit
