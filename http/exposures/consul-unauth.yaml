id: consul-unauth

info:
  name: Consul Unauthenticated Access
  author: internal
  severity: high
  description: Detects Consul API exposed without authentication allowing service enumeration.
  metadata:
    max-request: 1
  tags: http,exposure,consul

http:
  - method: GET
    path:
      - "{{BaseURL}}/v1/agent/services"
    matchers:
      - type: word
        part: body
        words:
          - '"Service"'
          - '"ID"'
        condition: and

      - type: status
        status:
          - 200
        condition: and
