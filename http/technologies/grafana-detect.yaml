# Source: https://github.com/projectdiscovery/nuclei-templates/blob/main/http/technologies/grafana-detect.yaml
# Modified: 收敛 matcher

id: grafana-detect

info:
  name: Grafana Detect
  author: pdteam
  severity: info
  description: Detects Grafana monitoring dashboard.
  metadata:
    max-request: 1
  tags: fingerprint,http,grafana,panels

http:
  - method: GET
    path:
      - "{{BaseURL}}/login"
    matchers:
      - type: word
        part: body
        words:
          - "Grafana"
          - "grafana_app"
        condition: or
