id: gitlab-detect

info:
  name: GitLab Detect
  author: internal
  severity: info
  description: Detects GitLab via title or CSRF token pattern.
  metadata:
    max-request: 1
  tags: fingerprint,http,gitlab,devops

http:
  - method: GET
    path:
      - "{{BaseURL}}/users/sign_in"
    matchers:
      - type: word
        part: body
        words:
          - "GitLab"
          - "csrf-token"
        condition: and
